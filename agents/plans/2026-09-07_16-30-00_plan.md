# Event Forms Management Task 2 Implementation Plan

## Goal

Add the secure admin setup page at `/admin/events/:id/forms/new` for naming an event form and uploading its blank PDF, then create the persisted form with an empty valid definition and redirect to the later edit route.

## Scope and files

- Create `src/routes/(secure)/admin/events/[id]/forms/new/+page.server.ts` with event loading and a validated upload action.
- Create `src/routes/(secure)/admin/events/[id]/forms/new/+page.svelte` with Bootstrap-only name/PDF setup UI.
- Create `src/routes/(secure)/admin/events/[id]/forms/new/forms-new-page.test.ts` covering server behavior and markup.
- Create `.superpowers/sdd/2026-09-07-event-forms-management/task-2-report.md` with implementation evidence.

## Implementation checklist

- [x] Add focused failing tests first and verify the expected missing-route failure.
- [x] Implement event load, name/PDF validation, R2 upload, D1 insert, and edit-route redirect.
- [x] Implement the setup page with only name and blank PDF controls; do not add custom CSS or designer code.
- [x] Run focused tests, type checks/lint/build as practical, and inspect the diff.
- [x] Complete this plan's review section, write the required report, and commit the implementation.

## Review

Implementation verified with focused Vitest, full `npm test`, targeted Prettier/ESLint, `git diff --check`, and `npm run build`. `npm run check` reports two pre-existing errors in the logout test and Task 1 forms list link, plus existing warnings; no new diagnostics remain in the Task 2 files.
