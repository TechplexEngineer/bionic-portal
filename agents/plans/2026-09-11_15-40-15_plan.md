# Clarify new-account creation on login

- [x] Inspect the login and magic-link account-creation path and choose the smallest user-facing warning.
- [x] Add a clear, accessible warning near the email field explaining that an unrecognized address creates a new account, so typos should be corrected.
- [x] Add or update focused regression coverage for the login UI/behavior if the existing test setup supports it.
- [x] Run targeted tests plus type/build and diff checks; record results and any pre-existing failures.
- [ ] Commit the verified change and sync the feature branch and local `main` as required by repository workflow.

## Review

- Added a Bootstrap warning above the login email field explaining that an unrecognized email creates a new account and asking users to check for typos.
- Added a regression test that keeps the warning copy and semantic alert markup present.
- Verification: targeted login tests pass (14 tests); all unit tests pass (134 tests); `npm run check` and `npm run build` pass with existing warnings; `git diff --check` passes.
- `npm run lint` remains blocked by 45 pre-existing repository-wide Prettier violations; the changed files are not among the reported files.
- Commit and branch/main synchronization pending.
