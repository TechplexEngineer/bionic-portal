# Brevo magic-link authentication

- [x] Inspect existing authentication and create isolated feat/brevo-magic-links worktree.
- [x] Replace code requests with cryptographically random, hashed, 15-minute magic links; atomically consume tokens; preserve existing users, roles, and sessions.
- [x] Use Brevo API with BREVO_API_KEY and Team 4909 No Reply <no-reply@team4909.org>; fail closed on missing configuration or delivery errors; add resend cooldown.
- [x] Replace code-entry UI with email request and explicit link confirmation (GET must not consume tokens).
- [x] Remove obsolete password controls in admin user management while retaining database compatibility.
- [x] Add authentication and email tests; run tests, type checking, build, and review diff.

Reuse magic_codes storage with code containing a SHA-256 token digest (no schema migration). Old six-digit codes cannot pass token validation. Consume via DELETE RETURNING to prevent replay. Link confirmation uses same-origin POST and no-store/referrer protections. No live email is required for verification.

## Review

- 30 focused tests pass (token storage with real SQLite, route actions, Brevo payload/failures, admin account actions).
- Production Cloudflare build passes; targeted ESLint and diff whitespace checks pass.
- Type checking has the same 32 pre-existing errors as main, with no new errors; warnings decrease from 4 to 3.
- Full server suite is blocked on both main and this branch by existing missing @testing-library/svelte dependency.
- No live email sent or deployment performed. BREVO_API_KEY and approved Brevo sender are required at runtime.
- Existing magic_codes table is reused, so no migration is required. Existing roles are preserved, including mixed-case legacy emails; ambiguous legacy accounts fail closed.
- Centralized response headers avoid duplicate SvelteKit header assignments; removed platform logging that could expose environment secrets.
- Final review corrected test fixture types and lint issues in touched files.
