# Explain student edit failures

- [x] Trace the student edit action and identify why the user sees only a generic failure.
- [x] Add a failing regression test proving the database failure reason is returned to the form.
- [x] Implement the smallest safe error-message change without exposing sensitive details.
- [x] Run focused tests, type checks, formatting checks, and inspect the final diff.
- [x] Record verification results and review notes here.

## Review

The student edit action now appends the caught `Error` message to the form failure response, so admins see the database reason instead of only "Failed to update student". Non-`Error` failures use an "Unknown database error" fallback, and the existing server-side log remains in place.

Verification:

- Focused student-edit test: passed (4 tests).
- Full unit suite: passed (15 files, 54 tests).
- `npm run check`: passed with 0 errors and 27 pre-existing warnings.
- `npm run build`: passed; existing Svelte warnings were emitted.
- Targeted Prettier check: passed.
- `git diff --check`: passed.
