# Login email address note

- [x] Add a note on the login page linking to `http://billericak12.com`.
- [x] Verify formatting and project checks.

## Review

- Added the note below the login introduction using existing Bootstrap utility classes.
- `npm run check` passed with 0 errors; it reports 29 pre-existing warnings in unrelated files.
- `npm run lint` remains blocked by 39 pre-existing Prettier violations in unrelated files.
- The changed Svelte file passes `npx prettier --check src/routes/login/+page.svelte`.
