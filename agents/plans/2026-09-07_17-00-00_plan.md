# Task 3: Event form editor

- [x] Add a focused failing test for the editor server load/action and page markup.
- [x] Implement the relationship-scoped editor server load/action.
- [x] Implement the full-width PdfFormDesigner page and save form.
- [x] Run focused tests, type checks, and formatting checks.
- [x] Review the diff, record results and concerns, then commit the changes.

## Review

Implemented the event-scoped editor route. Focused and full unit tests, production build,
formatting, and diff checks pass. `npm run check` remains non-zero because of two
pre-existing unrelated errors in `src/routes/logout/logout.server.test.ts` and the
existing forms list `resolve()` call; the new editor contributes only the existing
Svelte state-capture warning pattern.
