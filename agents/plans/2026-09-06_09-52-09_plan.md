# Shop inventory database and admin management

- [x] Add a `shop_locations` Drizzle table and a D1 migration seeded with the existing shop inventory.
- [x] Load shop locations from the database for attendance search, preserving fuzzy item search and empty-state behavior.
- [x] Add authenticated admin CRUD for shop items and their locations with server-side validation.
- [x] Add the shop management page to admin navigation and build a Bootstrap-based editing interface.
- [x] Add focused tests for shop data validation/search and run type checks, unit tests, and formatting checks.
- [x] Review the diff and record verification results here.

## Review

Implemented the `shop_locations` D1 table and seeded all existing shop locations. Attendance search now reads from D1, while `/admin/shop` provides validated create, update, and delete actions and is linked from the admin navigation. Local migrations, `npm run check`, 52 unit tests, and `npm run build` all pass. Repository-wide `npm run lint` remains blocked by pre-existing formatting and lint errors in unrelated files; changed files pass targeted Prettier checks, and `git diff --check` is clean.
