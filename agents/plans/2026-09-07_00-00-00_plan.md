# Role-aware navbar and admin protection

## Plan

- [x] Add failing coverage for logged-in dashboard navigation and admin-only menu visibility.
- [x] Add failing coverage for admin layout authorization of anonymous and non-admin users.
- [x] Implement role-aware navbar links using existing Bootstrap patterns.
- [x] Enforce the admin role in the admin server layout while preserving login redirects.
- [x] Run focused tests, type checks, formatting/lint checks, and inspect the final diff.

## Review

Implementation is complete. Unit tests, type checking, build, and changed-file formatting checks pass. The repository-wide lint command remains nonzero because of pre-existing formatting and header lint findings outside this change; see the handoff summary.
