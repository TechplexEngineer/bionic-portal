# Add mentor registration link

- [x] Inspect the landing page and authentication routes to identify the correct mentor registration destination.
- [x] Add a clearly labeled mentor registration link to the landing page using existing Bootstrap utilities.
- [x] Run formatting/type checks and inspect the final diff.
- [x] Document verification results.

## Review

- Added `Are you a mentor? Register here` to the landing page, linking to `/login`, the app's account creation/sign-in flow.
- `npm run test:unit -- --run` passed: 25 files, 102 tests.
- `npm run build` passed.
- `git diff --check` and Prettier check passed for the changed Svelte file.
- `npm run check` remains blocked by the pre-existing `src/routes/logout/logout.server.test.ts` type error; no errors were reported in the changed landing page.
