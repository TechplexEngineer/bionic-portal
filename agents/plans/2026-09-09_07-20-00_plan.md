# Add calendar embed to dashboard

## Goal

Add the existing Team 4909 Google Calendar embed below the current content on the authenticated dashboard page.

## Plan

- [x] Confirm the dashboard route and reuse the existing calendar embed configuration.
- [x] Add a responsive calendar section below both dashboard role variants.
- [x] Run formatting/type checks and inspect the final diff.
- [x] Document verification results.

## Review

- Added the existing Team 4909 Google Calendar embed below the authenticated dashboard content.
- Used Bootstrap's responsive `ratio ratio-16x9` utility rather than adding custom CSS.
- `git diff --check`: passed.
- `npm run build`: passed.
- `npm run check`: blocked by the pre-existing `src/routes/logout/logout.server.test.ts` type error; it also reports existing warnings unrelated to this change.
