# Sort landing-page events by start date

- [x] Add a regression test for the landing page server load with events returned out of order.
- [x] Run the focused test and confirm it fails because the landing page preserves database order.
- [x] Apply the existing `sortEventsByStartDate` helper in the landing page server load.
- [x] Run focused tests, type checking, and linting.
- [x] Review the diff and document verification results.

## Review

Implemented the landing-page server load using `sortEventsByStartDate`, with a co-located regression test covering out-of-order database results.

Verification:

- `npm run test:unit -- --run`: passed, 15 test files / 52 tests.
- `npm run check`: passed with 0 errors and 26 pre-existing warnings.
- `npm run build`: passed; existing warnings about Svelte state references, unused imports, and missing QuickBooks environment variables remain.
- Targeted ESLint and Prettier checks for changed files: passed.
- Full `npm run lint`: not clean because the repository contains unrelated existing Prettier violations in 41 files.
