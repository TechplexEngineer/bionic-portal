# Show logged-in email in navbar user dropdown

- [x] Inspect the shared navbar, authentication user shape, and existing header tests.
- [x] Render the authenticated user's email in the navbar dropdown using the existing user identity field.
- [x] Add a focused regression assertion for the email item.
- [x] Run formatting, type checks, focused tests, and diff validation.
- [x] Record review results and commit the verified change.

## Review

- Added the authenticated user's `username` (the normalized login email) as a Bootstrap `dropdown-item-text` entry in the navbar user menu.
- Added a focused header regression assertion.
- Passed targeted Prettier, `git diff --check`, focused header tests, full unit tests (28 files, 118 tests), `npm run check` (0 errors, existing warnings), and `npm run build`.
- `npm run lint` remains blocked by pre-existing repository-wide Prettier drift in 46 unrelated files; the changed files pass targeted formatting.
