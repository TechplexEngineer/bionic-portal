# Plan: Dynamic student edit form

- [x] Inspect the student schema, edit page, server load/action, and related admin form conventions.
- [x] Design the smallest schema-driven form/update path that renders and persists every student field safely.
- [x] Implement the dynamic form and server-side validation/update behavior.
- [x] Add or update focused tests for rendering and persistence, then run the relevant test/type checks.
- [x] Review the diff and document verification results.

## Review

- The student edit page now derives field descriptors from Drizzle's `students` table metadata, so schema fields are rendered without duplicating form code.
- The default action parses booleans/numbers, validates required columns, updates every field, and supports changing `userid` by redirecting to its new URL.
- Focused tests passed: 3/3.
- `npm run build` passed. It still emits existing warnings for unrelated routes and missing optional QuickBooks environment variables.
- `npm run check` remains blocked by pre-existing errors in unrelated files/configuration; the initial student route metadata comparison error was fixed and the changed route was absent from the filtered rerun diagnostics.
- `git diff --check` passed.
