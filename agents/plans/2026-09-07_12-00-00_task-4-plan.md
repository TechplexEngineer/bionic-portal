# Event Forms Management Task 4 Implementation Plan

**Goal:** Verify that event creation and existing admin registrations navigation land on the event forms list, then run the complete verification suite.

**Architecture:** Inspect only the two navigation files named by the task brief. Preserve the existing event-scoped admin authorization, R2 key layout, signed submissions, Bootstrap-only styling, and migration-free implementation. Make no source changes if both destinations already match the brief.

**Tech Stack:** SvelteKit, TypeScript, Svelte, npm, Vitest, Vite, svelte-check, Git.

**Spec:** `.superpowers/sdd/2026-09-07-event-forms-management/task-4-brief.md`

## Global Constraints

- Modify only `src/routes/(secure)/admin/events/add/+page.server.ts` and `src/routes/(secure)/admin/events/[id]/registrations/+page.svelte` if navigation does not already match.
- Update event creation and existing admin links to land on the list page.
- Preserve existing event-scoped admin authorization, R2 key layout, signed submissions, Bootstrap-only styling, and no migration.
- Run the full unit suite and production build.
- Run `npm run check` and record only known pre-existing diagnostics if present.
- Run `git diff --check` and inspect the final route tree.
- Do not rewrite shared history.

---

### Task 4: Verify or rewire navigation

**Files:**
- Inspect/Modify: `src/routes/(secure)/admin/events/add/+page.server.ts`
- Inspect/Modify: `src/routes/(secure)/admin/events/[id]/registrations/+page.svelte`
- Create: `agents/plans/2026-09-07_12-00-00_task-4-plan.md`
- Create: `.superpowers/sdd/2026-09-07-event-forms-management/task-4-report.md`

- [x] **Step 1: Read the task brief and repository lessons.**

- [x] **Step 2: Inspect the two navigation files and confirm their destinations.**

- [x] **Step 3: Make the minimal navigation edits only if a destination differs from `/admin/events/${id}/forms`.** No source edit was needed; both destinations already match.

- [x] **Step 4: Run the full unit suite and production build.** Unit suite passed; build passed with existing warnings/notices.

- [x] **Step 5: Run `npm run check`, `git diff --check`, and inspect the final route tree.** `npm run check` has two unrelated existing errors; diff check passed and the route tree contains the event forms list route.

- [x] **Step 6: Write the requested implementation report with every command, status/output, changed files, and concerns.**

- [x] **Step 7: Commit only task-owned changes without rewriting shared history.** Commit `35e1eb1` contains only the plan and requested report.

## Review

Review: Navigation matched the task brief before implementation, so neither listed source file changed. Verification evidence and pre-existing check concerns are recorded in the task report.
