# Plan: Add YOG and table search/filter controls

- [x] Add the students’ graduation year (YOG) to the student overview table.
- [x] Add reusable search and per-column filter controls to `TableForObjectArray`.
- [x] Add search/filter controls to custom data tables that do not use the shared component.
- [x] Add focused regression tests for shared table search/filter behavior.
- [x] Run type checks, focused tests, formatting checks, and inspect the final diff.

## Review

Implemented YOG on the student overview and added client-side search/filter controls to the shared data table and bespoke admin tables for shop locations, registrations, mail recipients, and carpool mentors.

Verification:

- `npm run test:unit -- --run src/lib/components/TableForObjectArray.test.ts`: 11/11 passed.
- `npm run test:unit -- --run`: 69/69 passed across 19 files.
- `npm run check`: passed with 0 errors and 29 existing warnings.
- `npx prettier --write` on changed files: passed.
- `git diff --check`: passed.
