# Filter inactive students from `/attend`

- [x] Inspect the attendance loader and existing student visibility rules.
- [x] Add server-side filtering so hidden and graduated students are omitted from `/attend`.
- [x] Add a focused regression test covering hidden, graduated, and active students.
- [x] Run formatting, type checks, and the relevant unit tests; review the diff.
- [x] Commit the verified changes, push the feature branch, merge into local `main`, and push `main`.

## Review

Updated `/attend` to omit hidden students and students whose graduation year is the previous calendar year before building either attendance list. Added a regression test proving both excluded categories stay out while an active student remains visible.

Verification: focused attendance unit tests pass (2 tests), targeted Prettier and ESLint pass, `npm run check` passes with 0 errors and 29 existing Svelte warnings, and `git diff --check` passes. Repository-wide `npm run lint` remains blocked by 38 pre-existing formatting violations outside this change.
