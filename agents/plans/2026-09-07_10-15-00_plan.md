# Local Startup Migration Race Plan

**Goal:** Remove the duplicate local D1 migration that causes startup failures such as `attendance already exists` and downstream HTTP 500s.

## Checklist

- [x] Trace startup scripts and reproduce the competing migration paths.
- [x] Keep local database initialization owned by `npm run dev` only.
- [x] Remove redundant migration setup from Superset and Playwright startup wrappers.
- [x] Verify shell syntax, E2E startup, type checking, and the working tree diff.

## Review

- `.superset/run.sh` previously started `npm run dev` in the background and then ran `db:migrate:local`; `npm run dev` now already performs reset, migration, and fixture seeding, so the two migrations raced against the same local D1 database.
- Removed the second migration call from `.superset/run.sh` and the redundant pre-migration from `playwright.config.ts`.
- `bash -n` passed for startup scripts, `npm run test:e2e` passed (1 test), and `npm run check` passed with 0 errors and 29 pre-existing warnings.
