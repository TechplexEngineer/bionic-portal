# Event Forms Management Final Review Fix Plan

**Goal:** Point the event forms list View affordance at the implemented authenticated base-PDF route and verify the final event-forms flow.

**Architecture:** Preserve the existing SvelteKit `resolve(...)` route construction and edit destination. Change only the View link and its focused source-level regression assertion; append verification evidence to the existing Task 1 report.

**Tech Stack:** SvelteKit, Svelte, TypeScript, Vitest, Vite, svelte-check, npm, Git.

**Spec:** `.superpowers/sdd/2026-09-07-event-forms-management/task-1-brief.md`, `.superpowers/sdd/2026-09-07-event-forms-management/review-3340c0b..5cff283.diff`

## Global Constraints

- Use the authenticated relationship-scoped `/admin/events/{id}/forms/{formId}/base` endpoint for View.
- Keep Edit targeting `/admin/events/{id}/forms/{formId}/edit`.
- Do not change unrelated files or rewrite history.
- Run the focused forms-list test, forms-related tests, `npm run build`, and `npm run check`.
- Append changed files, exact command outcomes, and concerns to the existing Task 1 report when available.

---

### Task 1: Repair the View destination and verify

**Files:**
- Modify: `src/routes/(secure)/admin/events/[id]/forms/+page.svelte`
- Modify: `src/routes/(secure)/admin/events/[id]/forms/forms-page.test.ts`
- Modify: `.superpowers/sdd/2026-09-07-event-forms-management/task-1-report.md`
- Create: `agents/plans/2026-09-07_22-48-05_plan.md`

- [x] Update the focused assertion to require the implemented `/base` View route while retaining the Edit assertion.
- [x] Run the focused forms-list test and confirm it fails against the current unimplemented View route.
- [x] Change the View link to `resolve(`/admin/events/${data.event.id}/forms/${savedForm.id}/base`)`.
- [x] Run the focused list test, all forms-related tests, build, check, and diff validation; record exact outcomes.
- [x] Append the fix report to the existing Task 1 report with changed files, commands/output, and concerns.
- [x] Commit only the fix-wave changes.

## Review

The View link now targets the existing authenticated relationship-scoped base-PDF endpoint. Focused and forms-related tests pass, and the production build passes. `npm run check` no longer reports the forms-list route typing error; it remains non-zero only for the unrelated logout test type mismatch and existing warnings.
