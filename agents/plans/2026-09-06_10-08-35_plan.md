# Shop editor draft-state follow-up

- [x] Add failing tests for bulk-save validation and draft dirty-state tracking.
- [x] Implement a reusable draft-state helper and a validated `updateAll` server action.
- [x] Bind admin row inputs to drafts, preserve values after individual saves, and highlight edited fields/rows.
- [x] Add Save All and browser/internal navigation warnings for unsaved changes.
- [x] Run focused red/green tests, full checks/build, review the diff, then commit, push, and merge to `main`.

## Review

The editor now keeps row drafts in state and uses `reset: false` after enhanced saves, so saved values remain visible. Changed location/item fields receive Bootstrap warning highlighting; Save All submits all drafts through a validated server action; SPA and browser unload navigation both warn when drafts are dirty. Full `npm test` passes: 15 test files, 56 unit tests, and 1 e2e test. `npm run check` reports 0 errors (32 existing warnings), the build passes, and `git diff --check` is clean.
