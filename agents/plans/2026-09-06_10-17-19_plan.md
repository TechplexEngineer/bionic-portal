# Open the dev server in a new Superset browser tab

- [x] Inspect the existing Superset run script and browser CLI contract.
- [x] Add a readiness check and open the workspace application URL in a new browser tab.
- [x] Validate shell syntax, diff cleanliness, and the resulting script behavior without leaving a dev server running.
- [x] Record verification results and review notes here.

## Review

Implementation is complete. `.superset/run.sh` now waits for the selected Vite URL to respond, then invokes `superset browser open` with the current workspace ID and `--target new-tab`. Browser-open failures are reported without stopping the dev server.

Verification:

- `bash -n .superset/run.sh`: passed.
- `git diff --check`: passed.
- `superset browser --help` and `superset browser open --help`: confirmed the required command and flags.
- Final review confirmed `APP_URL` is computed after any port collision increment, so the opened URL and `.superset/workspace.env` always match the actual server port.
