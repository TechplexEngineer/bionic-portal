# Multiple student rows in parent registration

- [x] Inspect the parent registration UI/action and identify the smallest compatible batch-submission shape.
- [x] Add repeatable student email rows with add/remove controls and accessible labels.
- [x] Update the server action to validate and link all submitted student emails without partial writes from invalid input.
- [x] Extend focused server tests for multiple links and invalid batch behavior.
- [x] Run formatting, type checking, lint/targeted tests, and review the final diff.
- [ ] Commit the verified change and sync the feature branch and local `main` as required by repository workflow.

## Review

- Parent registration now renders one student email row by default, supports adding/removing rows, and submits repeated `studentEmail` fields.
- The server resolves all unique submitted emails before writing, then creates one conflict-safe link per student.
- Verification: focused tests and all 132 unit tests pass; `npm run check` and `npm run build` pass with existing Svelte warnings; `git diff --check` passes.
- `npm run lint` remains blocked by 45 pre-existing repository-wide Prettier violations.
