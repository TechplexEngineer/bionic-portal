# Plan: Simplify home page event layout

- [x] Inspect the home page markup and identify the audience cards, upcoming-events list, and calendar layout.
- [x] Remove the “For Parents” and “For Students” cards and place the calendar beside the upcoming-events list in a responsive Bootstrap row.
- [x] Run formatting/type checks and inspect the final diff for unintended changes.

## Review

Implemented in `src/routes/(root)/+page.svelte` using a responsive `row g-4` with two `col-md-6` columns. Removed the audience-card snippet because it is no longer referenced.

Verification:

- `npx prettier --check 'src/routes/(root)/+page.svelte' agents/plans/2026-09-06_10-08-32_plan.md` passed.
- `npm run check` passed with 0 errors; existing unrelated Svelte warnings remain.
- `npm run build` passed; existing unrelated Svelte warnings and missing optional QuickBooks environment variables were reported.
