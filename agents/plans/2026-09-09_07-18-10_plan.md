# Remove attend middle-column height constraint

- [x] Inspect the `/attend` middle-column layout and identify the height constraint.
- [x] Remove the height constraint without changing the events content or other columns.
- [x] Run focused formatting, type/check, and diff validation.

## Review

- Removed the `overflow-y-auto` class and `max-height: 50vh` inline style from the `/attend` events column wrapper.
- `npx prettier --check src/routes/attend/+page.svelte`: passed.
- `git diff --check`: passed.
- `npm run build`: passed.
- `npm run check`: blocked by the pre-existing type error in `src/routes/logout/logout.server.test.ts`; existing Svelte warnings remain elsewhere.
