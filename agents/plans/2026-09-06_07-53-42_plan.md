# Plan: Hide attendance entries before the season end

- [x] Locate all attendance-entry queries, API responses, and UI render paths.
- [x] Add a July 31 season cutoff at the narrowest shared data boundary, preserving July 31 entries.
- [x] Add or update regression coverage for entries before, on, and after the cutoff.
- [x] Run focused checks/tests plus project type/lint validation where practical.
- [x] Review the diff and record results here.

## Review

Implemented an inclusive `2026-07-31` cutoff for the admin attendance history and attendance summary API. The focused boundary test, Prettier check, ESLint check, and `git diff --check` pass. The full unit suite still has 9 existing DOM-test failures because it runs without a DOM environment, and `npm run check` reports unrelated pre-existing diagnostics elsewhere in the repository.
