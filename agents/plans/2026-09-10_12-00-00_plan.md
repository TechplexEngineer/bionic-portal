# Parent registration and navigation plan

- [x] Update the landing page labels and registration cards: “Returning User”, parent registration, and no mentor card in the primary options.
- [x] Add a site footer with a mentor registration link while keeping mentor registration available to authenticated users.
- [x] Ensure the parent registration flow clearly creates/uses the parent account and links it to an existing student by school email, with validation and useful feedback.
- [x] Add or update focused tests for the landing page and parent registration action behavior.
- [x] Run formatting, type checks, and relevant unit tests; inspect the final diff.
- [ ] Review results in this plan, then commit and integrate the verified change according to repository workflow.

## Review

Implemented the landing-page terminology and parent card, added a global footer mentor link, clarified the parent registration UI, and added parent-linking tests. `npm run check`, the full unit suite (119 tests), build, and targeted lint for changed Svelte files pass. Repository-wide `npm run lint` remains blocked by pre-existing formatting and ESLint errors across unrelated files.
