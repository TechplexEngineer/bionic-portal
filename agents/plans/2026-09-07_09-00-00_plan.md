# Preserve `/register` Login Redirect Plan

## Goal

Return unauthenticated users to `/register` after successful magic-link login.

## Checklist

- [x] Add a regression test proving `/register` includes its return path.
- [x] Update the `/register` guard to use the shared safe login URL helper.
- [x] Run focused tests and project verification checks.
- [x] Review the diff and document results.

## Review

- `npm run test:unit -- --run src/routes/register/register.server.test.ts src/routes/login/login.server.test.ts`: 13 tests passed.
- `npm run check`: passed with 0 errors and 29 pre-existing Svelte warnings.
- `npm test`: 83 unit tests and 1 Playwright test passed.
- `git diff --check`: passed.
- The root cause was the standalone `/register` page guard bypassing the shared `getLoginUrl` helper.
