# Remove search from the attendance “Here” list

- [x] Add an opt-out prop to `TableForObjectArray` for tables that do not need search.
- [x] Disable search for the attendance page’s “Here” table while preserving it elsewhere.
- [x] Add/update component coverage for the opt-out behavior.
- [x] Run focused tests, type checks, and formatting validation.

## Review

Implemented `searchable={false}` for the attendance “Here” table. The shared table keeps
search enabled by default, and the new component test confirms the opt-out renders table
rows without a searchbox.

Verification:

- `npm run test:unit -- --run src/lib/components/TableForObjectArray.test.ts` — passed (13 tests).
- Prettier check and `git diff --check` — passed.
- `npm run check` — blocked by the existing `logout.server.test.ts` type error; it also reports existing warnings, with no diagnostics from the changed files.
