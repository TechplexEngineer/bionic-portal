# Resolve test suite failures

- [x] Run the unit and end-to-end test suites to capture the current failures.
- [x] Group failures by root cause and inspect the relevant implementation/tests/configuration.
- [x] Apply the smallest root-cause fixes, preserving unrelated working-tree changes.
- [x] Re-run targeted tests, then the complete available test suite and static checks.
- [x] Record the verification results and review notes here before handoff.

## Review

Implemented and verified:

- Unit suite: 14 files, 51 tests passed.
- `npm run check`: 0 errors; existing Svelte reactivity/accessibility warnings remain.
- `npm run build`: passed.
- E2E suite: 1 test passed with local D1 migrations.
- `git diff --check`: passed.

Root causes addressed were the component tests using the server project, missing DOM cleanup/matcher setup, stale schema/API field types, nullable data contracts, and E2E startup using Pages/remote migration behavior for a Worker project.
