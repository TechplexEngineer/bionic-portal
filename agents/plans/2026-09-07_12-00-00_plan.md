# Student event registration fix

## Plan

- [x] Trace the student event registration UI, server action, schema, and existing tests.
- [x] Identify the spinner/no-op root cause and the existing registration/action-item data model.
- [x] Implement registration creation plus conditional permission-form and payment action items, preserving duplicate safety and error handling.
- [x] Add or update focused server/UI coverage for successful registration and conditional action items.
- [x] Run focused tests, type checking, lint/format checks, and review the final diff.
- [ ] Commit and push the feature branch, merge it into local `main`, and push `main` if repository remotes/branch state permit.

## Review

- Registration now verifies the student profile before writing, so foreign-key failures become a visible profile-completion message.
- Registration flags are initialized from event requirements: free events are paid and events without a permission form are form-complete.
- The compete page renders failed action messages, while successful enhanced submissions invalidate the page and show the new registration.
- Verification passed: focused registration tests (3), full unit suite (86), `npm run check`, targeted Prettier, targeted ESLint, and `npm run build`.
- Full `npm run lint` remains blocked by 37 pre-existing repository-wide Prettier violations outside this change.
