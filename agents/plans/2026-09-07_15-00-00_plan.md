# Event forms and admin event operations

## Plan

- [x] Inspect and document the existing event/registration, auth, D1, and R2 seams.
- [x] Add the bionic-sign dependency and schema/migration for event forms and signed submissions.
- [x] Build the admin form creator flow: upload base PDF to R2, edit with `PdfFormDesigner`, and save the JSON definition in D1.
- [x] Build the student form-completion flow with `PdfFormFiller`, prefilled student data, and signed PDF upload to R2.
- [x] Replace event readiness’s single-form assumption with per-form completion status while preserving legacy events.
- [x] Add authenticated admin exports for event rosters and all signed forms, and link them from registrations.
- [x] Make manual admin enrollment explicitly available after the registration deadline with safe duplicate/error handling.
- [x] Add focused server/UI tests, run formatting, type checking, unit tests, and build checks.
- [x] Review the diff for scope, security, migration compatibility, and unused legacy behavior; leave changes uncommitted.

## Review

- Implemented locally. `npm run db:migrate:local`, production build, focused unit tests, and `git diff --check` pass.
- `npm run check` reports one pre-existing `logout.server.test.ts` typing error and existing Svelte warnings; no new diagnostics remain in the feature files.
- `npm run lint` remains blocked by pre-existing repository-wide Prettier drift; the vendored bionic-sign build output is excluded via `.prettierignore`.
- No commit or push was performed per request.
