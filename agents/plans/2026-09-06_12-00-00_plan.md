# Student list visibility and hidden toggle

- [x] Inspect the student list server load, page UI, schema, and existing tests.
- [x] Define the default visibility predicate for hidden students and last-year YOG students, keeping any intentional override behavior clear.
- [x] Add a secure server action and student-list control to toggle a student’s hidden status.
- [x] Add or update focused server/UI tests for filtering and toggling.
- [x] Run formatting, type checks, lint, and relevant tests; review the diff.
- [x] Commit the verified changes, push the feature branch, merge into local `main`, and push `main`.

## Review

Implemented server-side filtering based on the current year, with an explicit query-string view for archived rows so hidden students can be unhidden. Added an admin-only atomic `toggleHidden` action and row controls. `npm run check`, all 76 unit tests, and `npm run build` pass. `npm run lint` is blocked by pre-existing repository-wide Prettier violations outside this change.
