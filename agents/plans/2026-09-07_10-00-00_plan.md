# Local Development Fixture Plan

**Goal:** Ensure local development databases contain the checked-in sample data after a reset, with a documented command to seed an existing local database.

## Checklist

- [x] Confirm the current local D1 reset/import behavior and Wrangler command support.
- [x] Add a reliable local fixture seed script and wire it into the reset workflow.
- [x] Update fixture documentation with the supported commands and expected sample data.
- [x] Verify a fresh reset contains the fixture and run relevant project checks.
- [x] Review the diff and document results.

## Review

- `npm run db:reset` applied all 11 local migrations and imported the fixture successfully.
- Local D1 contains 5 students, 5 events, and 74 attendance rows; `PRAGMA foreign_key_check` returned no violations.
- `npm run check`: 0 errors and 29 pre-existing Svelte warnings.
- `npm run test:unit -- --run`: 83 tests passed.
- Startup smoke test: `npm run dev -- --host 127.0.0.1 --port 8799` reached Vite, and `curl http://127.0.0.1:8799/` returned HTTP 200 with fixture-backed event cards.
- `git diff --check`: passed.
