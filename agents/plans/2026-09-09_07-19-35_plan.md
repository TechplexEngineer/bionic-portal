# Add uncheck-in action to attend page

## Goal

Allow staff to remove a currently active attendance record from the attend page so a checked-in person can be returned to the not-here list.

## Plan

- [x] Add an uncheck-in form/action to the “Here” table.
- [x] Delete only the selected student’s attendance record from the active six-hour window.
- [x] Add server-action tests covering successful removal and invalid/missing records.
- [x] Run formatting, type checks, unit tests, and inspect the final diff.
- [x] Document verification results.

## Review

- Added an “Uncheck In” button for each person in the attend page’s “Here” table.
- Added a server action that removes only active attendance records in the existing six-hour window, preserving historical attendance.
- Added tests for successful removal and the no-active-record case.
- `npx prettier --check` on changed files: passed.
- `npm run test:unit -- --run`: passed, 25 files / 105 tests.
- `npm run build`: passed.
- `npm run check`: reports one pre-existing error in `src/routes/logout/logout.server.test.ts` and existing warnings; no error in changed code.
- `npm run lint`: blocked by pre-existing formatting issues across 43 files and existing lint errors in `src/routes/attend/+page.svelte`.
