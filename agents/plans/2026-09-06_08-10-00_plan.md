# Fix signout session invalidation

- [x] Add a regression test for the logout POST action, covering session invalidation, cookie deletion, and redirect.
- [x] Convert `/logout` from a state-changing page load to a POST action that always clears the auth cookie and invalidates an active session.
- [x] Change the header menu to submit logout via POST.
- [x] Run targeted tests, static checks, build, and the available end-to-end suite.
- [x] Record verification and review results here.

## Review

Implemented and verified:

- Logout is now a POST action that invalidates the active server session and clears the `auth-session` cookie before redirecting to `/login`.
- The header's Sign out control submits the POST action instead of changing state through a GET page load.
- Unit suite: 15 files, 52 tests passed.
- `npm run check`: 0 errors; 29 pre-existing Svelte warnings remain.
- `npm run build`: passed.
- `npm run test:e2e`: 1 test passed.
- Focused Prettier check for all changed files: passed.
- `git diff --check`: passed.
- Repository-wide `npm run lint` remains red because of 45 pre-existing formatting violations in unrelated files; no changes were made to clean those up.
