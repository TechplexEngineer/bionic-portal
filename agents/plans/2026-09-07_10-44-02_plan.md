# Fix logout CSRF origin failure

- [x] Trace the logout form, SvelteKit CSRF handling, deployment origin assumptions, and existing tests.
- [x] Implement the smallest secure fix that allows legitimate logout submissions without relying on an unverified deployment origin.
- [x] Add or update regression coverage for the failure mode and logout behavior.
- [x] Run focused tests, type checks, and lint/format validation.
- [ ] Review the diff, document results here, and commit/push/merge according to repository workflow.

## Review

The initial trusted-origin approach was rejected after `wrangler pages project list` showed no `bionic-portal` Pages project. The final fix restores logout to a same-site GET navigation and retains the POST action for compatibility, sharing one session invalidation implementation.

Verification:

- `npm run test:unit -- --run src/routes/logout/logout.server.test.ts` passed.
- `npm run build` passed (with existing Svelte warnings and missing optional QuickBooks environment variables).
- Changed-file Prettier check and `git diff --check` passed.
- Full `npm run check` remains blocked by 8 pre-existing `Readonly<Platform>` type errors in existing server files.
- Full `npm run lint` remains blocked by 37 pre-existing formatting findings in unrelated files.
