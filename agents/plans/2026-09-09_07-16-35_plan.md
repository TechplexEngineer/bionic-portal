# Mentor registration form

## Goal

Create a separate authenticated mentor registration form that collects mentor-specific contact and background information without changing the student profile flow.

## Plan

- [x] Add a `mentor_profiles` table and migration for the mentor fields.
- [x] Add `/register/mentor` load/action handling for create-or-update, validation, and mentor role promotion.
- [x] Build the Bootstrap-based mentor form with only registration details: name, phone, company, shirt size, and FIRST alumni status.
- [x] Add a homepage link to the separate mentor registration path.
- [x] Add focused server and markup tests.
- [x] Run focused tests, type checking, build, formatting/lint checks, and inspect the final diff.

## Review

- `npm run test:unit -- --run` passed: 26 files, 105 tests.
- `npm run build` passed.
- `git diff --check` passed.
- Focused mentor tests passed: 3 tests covering login redirect, save/role promotion, and required-field validation.
- `npm run check` still reports one pre-existing logout test type error and existing Svelte warnings; the new mentor files add no diagnostics.
- Targeted lint is clean for the new mentor route/server/test files; the full targeted set still reports legacy navigation/key warnings in the existing homepage and unused/`any` errors in the existing schema.
- Removed the three application-style fields from the mentor schema and added a follow-up migration to remove their legacy non-null columns without editing the previously applied migration.
- The unrelated generated `vendor/bionic-sign/.svelte-kit/` directory was left untouched.
