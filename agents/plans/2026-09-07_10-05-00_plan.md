# Development Email Login Plan

## Goal

Add a development-only login path that accepts an email address directly, creates or reuses the matching account, establishes the normal session, and skips magic-link delivery. Production must remain magic-link-only.

## Checklist

- [x] Add a shared email-to-user helper so development login and magic-link verification use identical account lookup/creation behavior.
- [x] Add a `devLogin` server action guarded by SvelteKit's `dev` flag, including safe return-path handling and session-cookie creation.
- [x] Show the direct email login form only in development while preserving the production magic-link UI.
- [x] Add focused tests for development login behavior and ensure magic-link behavior still passes.
- [x] Run formatting/type checks/tests, inspect the final diff, and document results here.

## Review

Implemented the development-only `devLogin` action and conditional login UI. The action validates and normalizes the email, reuses existing users/roles, creates new ordinary users when needed, sets the standard session cookie, and redirects only to a safe local return path. In non-development builds it returns 404, while the production form remains magic-link-only.

Verification:

- `npx prettier --check ...`: passed.
- `npm run check`: passed with 0 errors and 29 pre-existing Svelte warnings.
- `npm test`: 86 unit tests and 1 Playwright test passed; existing test stderr logs were expected assertions.
- `npm run build`: passed with existing Svelte/build warnings.
- `git diff --check`: passed.
