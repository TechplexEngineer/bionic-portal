# Repair missing student profile columns

- [x] Trace the reported D1 error through the schema and remote migration state.
- [x] Add a failing regression test requiring a new repair migration for missing student columns.
- [x] Add the repair migration without modifying the already-applied migration.
- [x] Apply and verify the migration locally and remotely.
- [x] Run unit tests, type checks, build, formatting, and diff checks.
- [ ] Commit, push the branch, merge into local `main`, and push `main`.
- [ ] Record verification results and review notes here.

## Review

The reported D1 error was caused by migration `0007` being edited after it had already been applied remotely. The remote migration ledger therefore considered the migration complete while the deployed table lacked `current_grade` and `gender`. Migration `0007` was restored to its original immutable contents, and migration `0010_repair_student_profile_columns.sql` adds the missing columns for already-deployed databases.

Verification:

- Regression test initially failed because migration 0010 was absent, then passed after adding it.
- Fresh local migration reset and replay applied all 11 migrations successfully.
- Remote migration applied `0010_repair_student_profile_columns.sql` successfully.
- Remote `PRAGMA table_info(students)` confirmed `current_grade` and `gender` exist.
- Full unit suite: passed (15 files, 55 tests).
- `npm run check`: passed with 0 errors and 27 pre-existing warnings. A concurrent check/build attempt had a generated-file race and was rerun serially.
- `npm run build`: passed with existing Svelte warnings.
- Targeted Prettier check: passed.
- `git diff --check`: passed.
