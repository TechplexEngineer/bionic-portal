# Navbar user dropdown alignment fix

- [x] Add a regression test proving the authenticated user menu is right-aligned.
- [x] Add Bootstrap's end-alignment utility to the navbar user dropdown.
- [x] Run the focused regression test, type checking, lint, and the full test suite.
- [x] Record the review results and any follow-up notes.

## Review

- Focused regression test: passed (1 test).
- Touched-file Prettier check: passed.
- Repository `check`, `lint`, full unit/test suite, build, and e2e checks remain blocked by pre-existing repository issues unrelated to this change.
- The fix is limited to Bootstrap's `dropdown-menu-end` utility on the authenticated user menu.
