# Plan: Add student registration free-text questions

- [x] Inspect the student registration UI, action, schema, and existing tests to identify the smallest compatible storage path.
- [x] Add the three requested free-text questions to the registration form and ensure saved answers are displayed when editing the profile.
- [x] Extend registration action coverage to prove all three answers are serialized and persisted through the existing custom-fields mechanism.
- [x] Run formatting/type checks and focused tests, then inspect the final diff for regressions.

## Review

- Added three optional free-text textareas to the student registration profile.
- Reused `students.customFields` with stable keys so answers persist without a schema migration.
- Preserved rendering of legacy custom fields and prevented duplicate rendering of the new questions.
- Added server coverage for serializing and persisting all three answers.
- Verification: focused registration tests pass (5/5), full unit suite passes (121/121), `npm run check` passes with 0 errors, touched-file formatting passes, targeted test ESLint passes, and `git diff --check` passes.
- Known repository baseline: `npm run lint` still fails because 45 unrelated files are not Prettier-formatted; `svelte-check` reports 39 pre-existing warnings but 0 errors.
