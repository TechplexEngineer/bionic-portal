# Remove student phone field and align registration styling

- [x] Inspect student registration form, server action, schema, and existing tests for phone-field behavior.
- [x] Remove student phone input/state/request handling while preserving parent phone collection and existing stored student phone values.
- [x] Replace page-specific form CSS with Bootstrap layout/form utility classes and remove inline sizing/style attributes.
- [x] Run formatting, Svelte checks, focused tests, build, and inspect the final diff.
- [x] Record verification results and review notes in this plan.

## Review

- Removed the student phone input, Svelte state, and form action handling. The legacy database column remains untouched, so existing stored values are preserved during profile updates.
- Kept parent/guardian phone fields and the existing server-side requirement for at least one parent phone number.
- Replaced custom registration CSS with Bootstrap container, grid, card, form, utility, and validation classes; removed inline styles.
- `npm run check`: passed with 0 errors; 26 existing state-reference warnings remain across the project, including the page's pre-existing `$state(data...)` pattern.
- `npm run test:unit -- --run`: passed, 14 test files and 51 tests.
- `npm run build`: passed. Existing configuration warnings for missing QuickBooks environment variables were emitted without failing the build.
- `npx prettier --check src/routes/register/+page.svelte src/routes/register/+page.server.ts`: passed.
- `git diff --check`: passed.
- `npm run lint`: blocked by repository-wide pre-existing formatting failures in 42 unrelated files; no changed files were listed among those failures.
