# Sort `/attend` upcoming events chronologically

- [x] Inspect the `/attend` load/render path and identify the narrowest sorting boundary.
- [x] Sort events by parsed `startDate` ascending before returning them to the page.
- [x] Add a regression test covering unsorted event input and expected soonest-first output.
- [x] Run focused tests plus typecheck/lint checks appropriate to the change.
- [x] Record verification results and review notes here.

## Follow-up: `/admin/events`

- [x] Add the admin events page to the same shared chronological sorter.
- [x] Add and run a regression test for the shared sorter.
- [ ] Commit, push, and merge into local `main`.

## Specification

The `/attend` page must display qualifying upcoming events in ascending chronological order by their `startDate`. Existing event filtering and event fields must remain unchanged. Events with valid ISO date strings are expected, matching the event schema validation.

## Review

- `npm run test:unit -- --run src/routes/attend/attend.server.test.ts`: passed (1 test).
- Shared event-sorting tests: passed (2 tests).
- Prettier check passed for the new test; the legacy server file retains its existing non-Prettier formatting to keep the diff minimal.
- `git diff --check`: passed.
- `npm run check`: blocked by 32 pre-existing errors in unrelated and existing files; no new diagnostic was reported for the sorting helper or its regression test.
- Full `npm test`: unit suite reached 37 passing tests but stopped on the pre-existing missing `@testing-library/svelte` dependency in `TableForObjectArray.test.ts`; e2e did not start.
- The implementation sorts a copied event array by parsed `dateStr`, preserving the input array and all existing page filtering behavior.
- The follow-up reuses the same copied-array sorter for `/admin/events` via a start-date accessor, preserving that route's existing event shape.
